# Handoff — Paris Group Copilot (2026-07-15)

> Objetivo: qualquer membro do squad (ou o agente numa nova sessão) retoma o
> trabalho sem precisar perguntar nada. Squad: Vilmar (frontend), Rafael
> (backend), Camila (produto/PM).

## Contexto (o que está acontecendo)

**Feito:**
- Repositório estruturado: Next.js + TS + Tailwind + App Router, rotas `/projeto` e `/hipotese`.
- Backend FastAPI em `api/` com entidades Projeto e Hipótese; contrato OpenAPI em `/docs` (6 endpoints); Docker Compose (Postgres + Redis) subindo sem erros.
- Enquadramento e arquitetura documentados (`docs/enquadramento.md`, `docs/arquitetura.md`).
- Padrões de repo adicionados: PR template e Issue template em `.github/`.

**Em andamento (TN-2):**
- Frontend ainda **não consome** o backend — as páginas mostram conteúdo estático.

**Risco atual:**
- As **falhas de LLM não estão cobertas** por teste (timeout > 10s, resposta fora de política). Só o caminho feliz foi exercitado — é o buraco que não pode ir pra produção silenciosamente.

## Estado das hipóteses

- **Hipótese central** (copiloto acelera formulação de hipótese: 40min → <10min): **ainda não testada** com usuários reais — falta o produto ter fluxo mínimo ponta a ponta.
- **Incerto:** se a página "Decisões Técnicas" (TN-3) contribui pra hipótese central ou é feature sem hipótese.

## Decisões pendentes

### 1. [TÉCNICA] Como o frontend consome o backend?
Opções: (a) Next.js chama o FastAPI (`api/`) direto do client, ou (b) via BFF nas API routes do Next.
**Por que bloqueia:** define o padrão de fetch/tipagem de **todas** as páginas de dados — construir as telas antes de decidir gera retrabalho garantido.

### 2. [PRODUTO] A página "Decisões Técnicas" entra no MVP?
Ela ajuda a **testar a hipótese central**, ou é escopo extra ("feature sem hipótese")?
**Por que bloqueia:** decide se a TN-3 é construída agora ou vai pro "Fora de Escopo" — e o Rafael/Vilmar não começam o scaffold sem essa resposta.

## Próximos passos

- **Camila:** decidir se "Decisões Técnicas" entra no MVP (decisão de produto #2) até **17/07**.
- **Vilmar:** propor o padrão de consumo do backend (decisão técnica #1) com um spike de 1 página até **17/07**.
- **Rafael:** adicionar teste de falha de LLM (timeout > 10s) no endpoint até **18/07**.
- **Demo:** gravar Loom de 2-3 min mostrando o `/docs` do FastAPI + fluxo atual — link vai aqui: [Loom pendente].
